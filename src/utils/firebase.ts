/**
 * Firebase Stub (Firebase removed in favor of Neon DB)
 */

export const auth = null;
export const db = null;
export const storage = null;
export const firebaseApp = null;
export function isFirebaseAvailable() {
  return false;
}
